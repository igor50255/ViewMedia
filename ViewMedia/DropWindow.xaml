<Window x:Class="ViewMedia.DropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Загрузить файл"
        Width="300" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStartupLocation="CenterOwner">

    <Border Background="White" CornerRadius="8"
            BorderBrush="#00838f" BorderThickness="2">
        <StackPanel>

            <!-- Заголовок — тащим за него -->
            <Border x:Name="PanelHeader"
                    Background="#00838f"
                    CornerRadius="5 5 0 0"
                    Padding="12 8"
                    Cursor="SizeAll"
                    MouseDown="PanelHeader_MouseDown">
                <Grid>
                    <TextBlock Text="Загрузить файл" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>
                    <Button x:Name="BtnClose" Content="✕"
                            HorizontalAlignment="Right"
                            Width="24" Height="24"
                            Background="Transparent" BorderThickness="0"
                            Foreground="White"
                            Cursor="Hand"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Зона drop -->
            <Border x:Name="DropZone"
                    Margin="12" Padding="0"
                    Height="100"
                    Background="Transparent"
                    BorderBrush="#00838f" BorderThickness="1"
                    CornerRadius="6"
                    AllowDrop="True"
                    Drop="DropZone_DropAsync"
                    DragOver="DropZone_DragOver"
                    DragLeave="DropZone_DragLeave">
                <Grid>
                <TextBlock x:Name="DropLabel"
                           Text="↓ Перетащите файл сюда"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#888888"/>
                    <Grid x:Name="ProgressIndicator" Visibility="Collapsed"
                          HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Ellipse Width="48" Height="48"
                                 Stroke="#cccccc" StrokeThickness="4"/>
                            <Ellipse Width="48" Height="48"
                                 Stroke="CornflowerBlue" StrokeThickness="4"
                                 StrokeDashArray="8 24"
                                 StrokeDashCap="Round"
                                 RenderTransformOrigin="0.5 0.5">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                         Storyboard.TargetProperty="Angle"
                                         From="0" To="360"
                                         Duration="0:0:1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </Grid>
                </Grid>
            </Border>
            <Grid>
                <TextBlock x:Name="nameFile" TextWrapping="Wrap" Margin="20, 0, 20, 15"></TextBlock>
            </Grid>

        </StackPanel>
    </Border>

</Window>
